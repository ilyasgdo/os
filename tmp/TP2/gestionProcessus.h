#ifndef GESTION_PROCESSUS_H
#define GESTION_PROCESSUS_H
// La chaîne de caractère précédée du numéro d'identification du processus.
int printPID(char * S);
// La chaîne de caractère précédée du numéro d'identification de son père.
int printPPID(char * S);
// La chaîne de caractère précédée des deux informations ci-dessus.
int printIDS(char * S);

#endif 